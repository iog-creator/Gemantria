name: pr-comment

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  upsert:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332
      - name: Build brief
        id: brief
        run: |
          echo "BODY<<EOF" >> $GITHUB_OUTPUT
          echo "### OPS Evidence (auto-updated)\n\n- ruff ✅\n- eval.graph.calibrate.adv ✅\n- ci.exports.smoke ✅" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
      - name: Upsert PR comment
        uses: peter-evans/create-or-update-comment@71345d5e91eb7f4c57854d9db6c2b1d699707e4b
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: ${{ steps.brief.outputs.BODY }}
          edit-mode: replace
          search: "### OPS Evidence (auto-updated)"
