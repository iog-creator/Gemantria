
## Repo Subsystem (NEW)

The `pmagent repo` subsystem implements repository-level introspection:

Commands:

* `pmagent repo semantic-inventory`
* `pmagent repo reunion-plan`
* `pmagent repo quarantine-candidates`

All commands are read-only and generate evidence under `evidence/repo/`.
