"""Services for interacting with external systems like LLMs, databases, etc."""

# ensure SimpleNamespace handling available to all services
# Ensure SimpleNamespace-safe text extraction across services
# SimpleNamespace handling is now handled in individual service files as needed
