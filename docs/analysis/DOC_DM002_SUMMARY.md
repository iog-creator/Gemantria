# DM-002 Document Classification Summary

This report is generated by `docs_dm002_summary.py`.
It provides an orchestrator-level view of the doc registry.

## Canonical vs Non-Canonical

- Canonical (`is_canonical = TRUE`): **177**
- Non-canonical (`is_canonical = FALSE`): **2001**
- Unset (`is_canonical IS NULL`): **0**

## Status Breakdown

- `archive_candidate`: **690**
- `canonical`: **177**
- `unreviewed`: **1311**

## Path Prefix Buckets

- docs/SSOT/** (SSOT): **35**
- archive/**: **1695**
- other: **448**

## Notes

- `status` values come from DM-001 (initially) and DM-002 sync.
- Future phases may introduce `needs_review` or `archived` statuses.
- This report is read-only; it does not change the DB or move files.
