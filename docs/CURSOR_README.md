# Cursor Bootstrap — Read Me First

**Reading order (required):**

1) `.cursor/rules/000-ssot-index.mdc`
2) `RULES_INDEX.md`
3) `AGENTS.md` (root), then `src/graph/AGENTS.md`, then `src/nodes/AGENTS.md`
4) `.cursor/rules/050-ops-contract.mdc`, `051-cursor-insight.mdc`, `052-tool-priority.mdc`
5) `.cursor/rules/060-pipeline-sequence.mdc`

**What to enforce:**

- Produce 4-block OPS replies (Goal, Commands, Evidence, Next gate) — Rule-051.
- Use the 8-node order in `src/graph/AGENTS.md` — Rule-060.
- Prefer repo + local tools per Tool Priority — Rule-052.

**If any file above is missing or unreadable:** STOP and request repair.
