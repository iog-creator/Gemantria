Revives 4 agents (planner/math/semantic/expert) from AGENTS.md, wires conditionally in graph.py via USE_AGENTS env (default false, preserves enrichment flow).

## Changes

- **New agent services:**
  - `src/services/planner_agent.py` - Determines processing sequence
  - `src/services/math_model_client.py` - Gematria calculation verification
  - `src/services/semantic_agent.py` - Embedding-based retrieval
  - `src/services/expert_agent.py` - Theological analysis

- **Graph integration:**
  - Added 4 agent node functions to `src/graph/graph.py`
  - Conditional wiring via `USE_AGENTS` env var (default: false)
  - Sequence: planner → math_agent → semantic_agent → expert_agent
  - Existing enrichment flow preserved when `USE_AGENTS=false`

## Evidence

- ✅ Agent imports successful
- ✅ Graph creation works (default and with USE_AGENTS=true)
- ✅ Agent edges wired when enabled
- ✅ Enrichment unaffected (completed successfully)
- ✅ No duplicates (Rule-054 verified)

## Testing

```bash
# Test imports
python3 -c "from src.services.planner_agent import plan_processing; ..."

# Test graph creation
USE_AGENTS=true python3 -c "from src.graph.graph import create_graph; g = create_graph()"
```

## References (REQUIRED)

- **Rules cited:** 050-ops-contract; 051-cursor-insight; 052-tool-priority; 054-reuse-first
- **Agents referenced:** AGENTS.md#orchestrator; AGENTS.md#cursor
- **Docs touched:** docs/SSOT/SHARE_MANIFEST.json; docs/forest/overview.md
- **SSOT links:** RULES_INDEX.md#050; AGENTS.md#workflow

## Scope (files only)

- src/services/planner_agent.py
- src/services/math_model_client.py
- src/services/semantic_agent.py
- src/services/expert_agent.py
- src/graph/graph.py

## Acceptance (governance v6.2.3)

- [ ] ruff format/check green (SSOT)
- [ ] `make ops.verify` passes (Rule-054 duplicate guard)
- [ ] Smokes relevant to this area green
- [ ] No new knobs or duplicate scaffolds

## Emitted Hints (REQUIRED)

- HINT: verify: agent wiring conditional OK
- HINT: verify: enrichment preserved

## Evidence tails

<!-- paste decisive tails or failing tails -->

