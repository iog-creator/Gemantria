# Auto-activate Gemantria virtual environment (direnv)
# Install direnv: https://direnv.net/docs/installation.html
# Then run: direnv allow

if [ -z "${VIRTUAL_ENV:-}" ] || [ "${VIRTUAL_ENV}" != "$(pwd)/.venv" ]; then
  source .venv/bin/activate
fi

